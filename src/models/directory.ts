export type Directory = {
  id: string;
  title: string;
  description: string;
  thumbnail: string;
};

export type Music = {
  id: string;
  audioSource: string;
  singer: string;
  thumbnail: string;
};
